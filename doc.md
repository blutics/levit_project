쿠팡의 경우에는 보안이 강화되어서 단순한 접근으로는 힘들고 해당 안티 봇들에 대응할 수 있는 메커니즘이 없는 경우에는 작업이 힘들다는걸 인지하고 진행하지 않았습니다.
그래서 일단은 어떤 보안장치가 가동되는지 알아보니 정확히는 알지 못하지만
akamai anti bot 시스템이 운용된다는걸 확인했습니다.

봇디텍션의 경우에는 왠만해서는 우회가 어렵고 여기에 들키지 않기 위해서는 어떤 툴들이 있는지 둘어보았습니다.
봇디텍션들은 보통 브라우저의 프로필을 확인하고 걸러내는걸로 알고 있어서
확실히 우회하지는 못하더라도 프로필만 잘 구성한다면 충분히 적은 개수에 대해서 작업은 가능할거라고 생각했습니다.

다양한 툴들을 둘러보았고.
SeleniumBase와 Playwright를 통해서 처리하는게 그 안정적으로 보였습니다.
정확히 기억나지는 않지만 기존에 사용하는 툴들도 있어 몇 가지를 선택해서
모두 사용해보고 이 조합을 사용하였습니다.

(보통 툴들은 개별적으로 프로필을 구성하여서 cdp를 통해 수집에 사용되는 브라우저의 프로필을 형성합니다.)

여기서 부터는 선택된 툴들을 통해서 수집을 시작하였습니다.
처음에는 프로그램을 실행시킬 때마다 프로필이 새로 만들어져 chrome_profile에 저장해두고 동일한 프로필을 사용할 수 있도록 작업하였습니다.
너무 자주 동일한 프로필에서 세션을 발생시키는 경우에 아카마이에 쉽게 걸리지 않을까하여 진행하였습니다.

확실히 처음보다는 걸리는 비율이 줄어보였습니다.

그리고 상품리스트와 상세페이지 수집이 확실히 안정적으로 되는 것을 확인하고는
혹시 requests와 같은 라이브러리를 통해서 브라우저 사용없이도 가능한지 확인하였습니다.
requests의 경우에는 http/2나 tls 연결을 지원하지 않아 다른 라이브러리를 사용했습니다.
진행이 되는 것으로 보여 비동기로도 진행해 보았으나 동시성을 증가시키니 어김없이 걸렸습니다.
그리고 테스트를 진행할수록 브라우저 없이 진행하는 수집은 확실히 아카마이에 걸리는 확률이 높아보였습니다.

이후에는 모두 브라우저를 통한 수집으로 진행하기로 정하였습니다.

여러 테스트를 진행하면서 예전에는 쿠팡이나 네이버에서 ip를 통한 차단을 진행한 반면
지금은 ip가 아니라 profile 단위로 차단을 진행한다는걸 확인할 수 있었습니다.
물론 ip 단위의 차단도 진행되기도 하겠지만 제가 체크한 바로는 profile 단위로 보였습니다.
하나의 프로필에서 차단되지만 크롬으로 다른 프로필의 크롬으로는 확실히 수집이 되는걸 확인하였습니다.

물론 이를 이용해서 다양한 테스트를 진행해볼 수도 있겠지만
시간이 많이 없는 시점에서 해볼 수 있는건 컨테이너 안에서 프로필이 생성되었을 때
아카마이가 다른 프로필로 인식한다면 안정적인 수집을 진행하 갈 수 있지 않을까라는 가정에 대한 테스트 였습니다.

그래서 쿠팡 수집도 마무리 되지 않고 네이버도 손대지 않은 시점에서 먼저 컨테이너로 띄우는 작업을 진행하였습니다.
이를 통해서 구현된 코드가 sb_example_2.py이고 docker-compose에서는 서비스를 올리면 이 코드가 작동 되도록 하였습니다.

짧게 정리하자면 이것도 아카마이 봇은 감지해냈으며 이후에는 혹시 다른 이미지 베이스면 어떨까를 확인해보았습니다.
하지만 이도 크게 영향을 미치지 못하였습니다.

여기까지가 저의 프로젝트의 진행과정이었습니다.
지속적으로 차단당하는 상황에서 다양한 테스트를 진행하다보니 코드 결과물이 적어 글로 풀어보았습니다.